<div>
  <div class="mt-2 bg-teal-100 rounded-3xl">
    <div class="mx-6 py-4">
      <h1 class="text-xl font-semibold text-dark-65">
        <%= medication_group.group_name %>
      </h1>

      <div class="mt-2 ml-2">
        <% medication_group.users.each do |user| %>
          <p class=" text-dark-65">
            <%= user.user_name %> 
          </p>
        <% end %>
      </div>
      <div class="flex items-center mt-4 gap-2">
        <%= link_to "編集", edit_medication_group_path(medication_group.id),class: "block mt-2 px-4 py-2 bg-sage-500 rounded-xl text-center hover:bg-sage-600 transition-colors" %>

        <div class="block mt-2 px-4 py-2 bg-sage-500 rounded-xl text-center hover:bg-sage-600 transition-colors">
          履歴
        </div>
      </div>
    </div>
  </div>
</div>